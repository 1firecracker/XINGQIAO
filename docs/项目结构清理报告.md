# 项目结构清理报告

## 📁 当前项目结构

### ✅ 正在使用的目录结构

```
XINGQIAO/
├── backend/                    # 后端服务（FastAPI）
│   ├── app/
│   │   ├── routes/            # API路由
│   │   ├── utils/             # 工具函数
│   │   └── ...
│   ├── tests/                 # 测试文件
│   └── requirements.txt
│
└── frontend/                   # 前端服务（React + Vite）
    ├── src/
    │   ├── api/               # API客户端
    │   ├── components/        # React组件
    │   ├── hooks/             # React Hooks
    │   ├── utils/             # 工具函数
    │   └── ...
    └── package.json
```

---

## 🗑️ 可以删除的文件/目录

### 1. **根目录下的旧文件**（已迁移到 frontend/）

这些文件是旧版本，现在项目使用 `frontend/` 目录下的文件：

- ❌ `App.tsx` → 使用 `frontend/src/App.tsx`
- ❌ `components/` 目录 → 使用 `frontend/src/components/`
- ❌ `constants.tsx` → 使用 `frontend/src/constants.tsx`
- ❌ `geminiService.ts` → 使用 `frontend/src/geminiService.ts`
- ❌ `index.html` → 使用 `frontend/index.html`
- ❌ `index.tsx` → 使用 `frontend/src/index.tsx`
- ❌ `types.ts` → 使用 `frontend/src/types.ts`
- ❌ `tsconfig.json` → 使用 `frontend/tsconfig.json`
- ❌ `vite.config.ts` → 使用 `frontend/vite.config.ts`
- ❌ `package.json` → 使用 `frontend/package.json`
- ❌ `package-lock.json` → 使用 `frontend/package-lock.json`

### 2. **未使用的代码文件**

- ❌ `frontend/src/hooks/useAI.js`
  - **原因**: 没有任何地方导入或使用
  - **替代**: 直接使用 `geminiService.ts` 中的函数

- ❌ `frontend/src/utils/constants.js`
  - **原因**: 没有被使用，常量定义在 `frontend/src/constants.tsx` 中
  - **注意**: 如果 `constants.tsx` 中缺少某些常量，需要先迁移

- ❌ `frontend/src/utils/helpers.js`
  - **原因**: `formatDate` 和 `calculateScore` 函数没有被使用
  - **建议**: 如果将来需要，可以保留，但目前未使用

- ❌ `backend/app/utils/cache.py`
  - **原因**: `ContentCache` 类没有被任何地方导入或使用
  - **注意**: 如果计划使用缓存功能，可以保留

### 3. **测试/文档文件**（可选删除）

- ⚠️ `backend/tests/test_image_generation.py`
  - **状态**: 独立的测试脚本，不是pytest测试
  - **建议**: 可以保留用于手动测试，或删除

- ⚠️ `backend/测试结果总结.md`
  - **状态**: 文档文件
  - **建议**: 可以保留作为历史记录，或移动到 `docs/` 目录

---

## 📋 清理步骤建议

### 第一步：删除根目录下的旧文件

```bash
# 删除根目录下的旧文件
rm App.tsx
rm -r components/
rm constants.tsx
rm geminiService.ts
rm index.html
rm index.tsx
rm types.ts
rm tsconfig.json
rm vite.config.ts
rm package.json
rm package-lock.json
```

### 第二步：删除未使用的代码文件

```bash
# 删除未使用的hooks
rm frontend/src/hooks/useAI.js

# 删除未使用的utils（如果确认不需要）
rm frontend/src/utils/constants.js
rm frontend/src/utils/helpers.js

# 删除未使用的后端工具
rm backend/app/utils/cache.py
```

### 第三步：更新 .gitignore（如果需要）

确保 `.gitignore` 包含：
- `.cursor/` 目录（调试日志）
- `debug.log` 文件

---

## ✅ 保留的重要文件

### 配置文件
- ✅ `.gitignore`
- ✅ `README.md`
- ✅ `docker-compose.yml`
- ✅ `env-example.txt`
- ✅ `start.ps1`, `start-backend.ps1`, `start-frontend.ps1`

### 文档文件
- ✅ `docs/` 目录下的所有文档
- ✅ `demo-script.md`

### 测试文件
- ✅ `backend/tests/test_ai.py`
- ✅ `backend/tests/test_scenarios.py`
- ✅ `backend/tests/test_training.py`
- ✅ `backend/tests/conftest.py`

---

## 🔍 需要进一步检查的文件

1. **`metadata.json`**
   - 检查是否被构建系统或部署脚本使用
   - 如果未使用，可以删除

2. **`frontend/public/index.html`**
   - 检查是否与 `frontend/index.html` 重复
   - 通常 Vite 使用根目录的 `index.html`

---

## 📊 清理后的预期结构

```
XINGQIAO/
├── .gitignore
├── README.md
├── docker-compose.yml
├── env-example.txt
├── start.ps1
├── start-backend.ps1
├── start-frontend.ps1
├── demo-script.md
├── metadata.json (需要确认)
│
├── backend/
│   ├── app/
│   │   ├── routes/
│   │   ├── utils/
│   │   │   └── file_manager.py (保留)
│   │   └── ...
│   ├── tests/
│   │   ├── test_ai.py
│   │   ├── test_scenarios.py
│   │   ├── test_training.py
│   │   └── conftest.py
│   └── requirements.txt
│
├── frontend/
│   ├── src/
│   │   ├── api/
│   │   ├── components/
│   │   ├── hooks/
│   │   │   ├── useScenarios.js (保留)
│   │   │   └── useTraining.js (保留)
│   │   ├── utils/ (可以删除整个目录，如果helpers.js和constants.js都不需要)
│   │   └── ...
│   └── package.json
│
└── docs/
    └── ...
```

---

## ⚠️ 注意事项

1. **备份**: 删除前建议先提交到git，或创建备份
2. **测试**: 删除后运行测试确保功能正常
3. **依赖检查**: 确认没有其他脚本或工具依赖这些文件
4. **缓存清理**: 删除后可能需要清理构建缓存

---

## 🎯 清理优先级

### 高优先级（安全删除）
- 根目录下的所有旧文件（App.tsx, components/, 等）
- `frontend/src/hooks/useAI.js`

### 中优先级（确认后删除）
- `frontend/src/utils/constants.js`
- `frontend/src/utils/helpers.js`
- `backend/app/utils/cache.py`

### 低优先级（可选）
- `backend/tests/test_image_generation.py`
- `backend/测试结果总结.md`
- `metadata.json`（如果未使用）
